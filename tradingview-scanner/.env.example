# Copy to .env and fill in. NEVER commit the real .env (see .gitignore).
# Telegram — from @BotFather (/newbot) and api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=123456789:ABCdefGhIJKlmNoPQRsTUVwxyZ
TELEGRAM_CHAT_ID=987654321

# Scanner A filter overrides (optional — defaults shown)
GAP_MIN=5
PRICE_MIN=3
VOL_MIN=50000
TOP_N=10
